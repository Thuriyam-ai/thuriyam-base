from typing import Optional
from pydantic import BaseModel, Field
from datetime import datetime

class {{ entity.title() }}Base(BaseModel):
    # TODO Add attributes from the model that will always be available in the schema of {{ entity.title() }}
    pass

class {{ entity.title() }}Create({{ entity.title() }}Base):
    """Schema for creating a new {{ entity.title() }} item"""
    pass

class {{ entity.title() }}Update(BaseModel):
    """Schema for updating a {{ entity.title() }} item (all fields are optional)"""
    pass

class {{ entity.title() }}Response({{ entity.title() }}Base):
    """Schema for {{ entity.title() }} item responses"""
    id: str
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True