from sqlalchemy import Column, String, Boolean
from core.base.model import BaseModel
from core.base.validator import Operation

class {{ (example_module_name[:-1] if example_module_name.endswith('s') else example_module_name) | title }}(BaseModel):
    __tablename__ = "{{ example_module_name }}"

    _unset = []
